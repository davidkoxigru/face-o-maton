<Window x:Class="face_o_maton.PhotoWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:face_o_maton"
        mc:Ignorable="d"
        Title="PhotoWindow"
        WindowStyle="None"  
        WindowState="Maximized">
    <Grid x:Name="GridPhoto">
        <Image x:Name="Photo"/>
        <Button Height="300" Width="300" Click="Photo_Button_Click">
            <Button.Template>
                <ControlTemplate>
                    <Image Source="pictures/Photo.png"/>
                </ControlTemplate>
            </Button.Template>
        </Button>

        <Button  x:Name="Button_cancel" Content="Button" Width="200" Height="200" HorizontalAlignment="Left" VerticalAlignment="Bottom" Click="Button_cancel_Click">
            <Button.Background>
                <SolidColorBrush Color="Red" Opacity="0.3" />
            </Button.Background>
        </Button>
    </Grid>
</Window>
